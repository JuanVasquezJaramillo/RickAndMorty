const initialState = {
    myFavorites: [],
    allCharacters: [],
    users: [],
    allCharactersFav: [],
}

const rootReducer = (state = initialState, {type, payload}) => {
    switch (type) {
        case payload:
            
            break;
    
        default:
            break;
    }
}

export default rootReducer;